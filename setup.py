from setuptools import find_packages, setup

setup(
    name='ada_portoSeguro',
    version='0.1.0',
    packages=find_packages(),
    description='A project for the ADA Porto Seguro competition.',
    author='Your Name' # Optional: kannst du deinen Namen eintragen
)